import valid_password
import validate_user


user = input("Ingrese nombre de usuario: ")

password = input("Ingrese una contraseña: ")

print(validate_user(user))
print(password_validation(password))
